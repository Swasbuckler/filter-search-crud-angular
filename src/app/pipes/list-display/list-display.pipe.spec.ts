import { ListDisplayPipe } from './list-display.pipe';

describe('ListDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ListDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
