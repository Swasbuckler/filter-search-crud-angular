import { ColumnTransformPipe } from './column-transform.pipe';

describe('ColumnTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
