import { ColumnCasePipe } from './column-case.pipe';

describe('ColumnCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnCasePipe();
    expect(pipe).toBeTruthy();
  });
});
