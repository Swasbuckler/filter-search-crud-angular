import { IsFilterEditPipe } from './is-filter-edit.pipe';

describe('IsFilterEditPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFilterEditPipe();
    expect(pipe).toBeTruthy();
  });
});
